{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build",
        "command": "task",
        "args": ["build"],
        "options": {
          "cwd": "${workspaceFolder}"
        }
      }
    ]
  }